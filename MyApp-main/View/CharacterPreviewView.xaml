<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyApp.View.CharacterPreviewView"
             xmlns:viewmodel="clr-namespace:MyApp.ViewModel"
             x:DataType="viewmodel:CharacterPreviewViewModel"
             Title="Aperçu du personnage">
    <VerticalStackLayout Padding="20" Spacing="20" HorizontalOptions="Center">
        <Image Source="{Binding Picture}" HeightRequest="300" Aspect="AspectFit"/>
        <Label Text="{Binding SpecialAttack}" FontAttributes="Bold" FontSize="24" />
    </VerticalStackLayout>
</ContentPage>
